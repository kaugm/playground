version: 2
deploy:
  steps:
    terraformPlan:
      before:
      - name: Checkov
        use: https://github.com/kaugm/env0-checkov-plugin
        inputs:
          directory: ./extra_checkov_checks 
          flags: --framework terraform
