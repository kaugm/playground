version: 1

deploy:
  steps:
    setupVariables:
      after:
        - pip3 install checkov
    terraformPlan:
      after:
        - cat $ENV0_TF_PLAN_JSON
        - checkov -f $ENV0_TF_PLAN_JSON --external-checks-dir extra_checkov_checks --check CKV_AWS_999
